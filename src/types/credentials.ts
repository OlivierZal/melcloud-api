// Thin re-export of @olivierzal/api-core's LoginCredentials (the
// core's session mechanism signs in with the pair and persists it, so
// the type must be the core's).
export type { LoginCredentials } from '@olivierzal/api-core'
