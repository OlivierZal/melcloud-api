// Thin re-export of @olivierzal/api-core's utility types (formerly
// heatzy-api's near-twin; `Resolved` originated here, and the core now
// carries both).
export type { Resolved, UndefinedTolerant } from '@olivierzal/api-core'
